// The function definition below determines the number of characters of a string.
// Which one of the following is correct in place of the missing statement?

int count_character(char *s)
{
    char *p;
    int count = 0;
    // missing statement
    return count;
}

A) for (p = s; p != '\0'; p++) count++;
B) for (p = s; p != "\0"; p++) count++;
C) for (p = s; *p != '\0'; p++) count++;
D) for (p = s; *p != "\0"; p++) count++;